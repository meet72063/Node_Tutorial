//if we use this code in utils without exporting the file with assigning name this result will show in the console 
//of utils

const num1=12;
const num2= 34;

function addValue(){
    console.log(`sum of the numbers is : ${num1+num2}`)
}

addValue()